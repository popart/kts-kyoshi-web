import React from 'react';
import { useParams } from 'react-router-dom';

export default function Chat() {
  /*
    let { id } = useParams();
      return <div>Now showing chat {id}</div>;
  */
  return (
    <div> Moshimosh! This is the Chat Page </div>
  )
};

